import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vn.holang.giapha',
  appName: 'Gia Phả',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
